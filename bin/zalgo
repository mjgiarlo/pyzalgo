#!/usr/bin/env python

import zalgo

if __name__ == '__main__':
    import sys
    if len(sys.argv) > 1:
        text = u' '.join(sys.argv[1:])
    else:
        text = sys.stdin.read()
    print zalgo.zalgo(text)
